# =============================================================
#  TheHive — secret.conf
#  Generate your key: openssl rand -hex 32
#  NEVER commit this file with a real key.
# =============================================================
play.http.secret.key = "CHANGE_ME_generate_with_openssl_rand_hex_32"
